#!/bin/bash

#
# Simple script that helps parallelise optipng.
#

cd "wallpapers/"
find . -type f -name "*.png" | parallel -j$(($(nproc)-2)) optipng -o7 -zm1-9 -strip all -fix -preserve -clobber -quiet
